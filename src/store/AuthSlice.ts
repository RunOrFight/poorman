import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  name: "",

};

const AuthSlice =  createSlice({
  name: 'auth',
  initialState,
  reducers: {

  },
});

export default AuthSlice